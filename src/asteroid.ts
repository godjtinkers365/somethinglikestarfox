import { MeshBuilder } from "@babylonjs/core"
export class Asteroid {
    container;
    constructor({}){}
    public render(scene){
        /*url_to_height_map,*/
        // this.container = MeshBuilder.CreateGroundFromHeightMap("gdhm","https://doc.babylonjs.com/_next/image?url=%2Fimg%2Fhow_to%2FHeightMap%2FheightMap.png&w=1920&q=75",{
        //     width:10,//	(number) size of the map width	10
        //     height:10,//	(number) size of the map height	10
        //     subdivisions:1,//	(number) number of map subdivisions	1
        //     minHeight:0,//	(number) minimum altitude	0
        //     maxHeight: 6,//	(number) maximum altitude	1
        //     // onReady	(function) a callback js function that is called and passed the just built mesh	(mesh) => {return;}
        //     // updatable	(boolean) true if the mesh is updatable	false
        // },scene)
    }
}